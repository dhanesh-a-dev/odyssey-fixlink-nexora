# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Database Connection (Zero-dependency SQLite local file)
DATABASE_URL="file:./dev.db"

# JWT Authentication Secret & Expiration
JWT_SECRET=fixlink_super_secret_jwt_key_2026
JWT_EXPIRES_IN=7d

# Cloudinary Credentials (Optional - if omitted, images save to backend/uploads/)
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
